package com.tomash.contactgetter.main;

/**
 * This enum contains field type that can be ignored while querying contacts.
 */

public enum FieldType {
    EMAILS,
    PHONE_NUMBERS,
    ADDRESS,
    WEBSITES,
    IM_ADDRESSES,
    SPECIAL_DATES,
    NOTES,
    RELATIONS
}
