package com.tomash.contactgetter.main;

/**
 * This enum contains field type that can be enabled while querying contacts.
 */

public enum FieldType {
    EMAILS,
    PHONE_NUMBERS,
    ADDRESS,
    WEBSITES,
    IM_ADDRESSES,
    SPECIAL_DATES,
    NOTES,
    RELATIONS,
    NICKNAME,
    ORGANIZATION,
    SIP,
    NAME_DATA,
    GROUPS
}
